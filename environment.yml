name: metageneration
channels:
  - conda-forge
  - bioconda
dependencies:
  - insilicoseq
  - python=3.10
  - pip
  - pip:
      - pandas
      - biopython=1.80
      - python-sat
      - scipy
      - pysam
      - future
      - joblib
